---
interface Props {
    plan: string;
    precio: string;
    mes: string;
    opcion: string
    des1: string;
    des2: string;
    des3: string;
    des4: string;
    des5: string;
    des6: string;
    des7: string;
    des8: string;
    des9: string;
    link: string;
}

const {
    plan,
    precio,
    mes,
    opcion,
    des1,
    des2,
    des3,
    des4,
    des5,
    des6,
    des7,
    des8,
    des9,
    link,
} = Astro.props;

import BotonCinco from '../BotonCinco.astro';

---

<section class="hover__planes">
    <article
        class="flex h-12 title__planes__descuento items-center  rounded-tl-lg rounded-tr-lg"
    >
        <p class="color__titulo  uppercase mb-5 font-semibold text-sm tracking-wider">DESCUENTO ESPECIAL</p>
    </article>

    <div class="rounded-bl-lg rounded-br-lg">
        <h2 class="font-bold text-3xl">{plan}</h2>
        <p class="text-4xl font-extrabold">{precio}</p>
        <p class="mb-5 text-sm font-semibold">{mes}</p>
        <p class="mb-5">{opcion}</p>
        <p class="underline text-sm font-semibold"> <strong>{des1}</strong> de espacio</p>
        <p class="text-sm font-semibold"> <strong>{des2}</strong> tráfico</p>
        <p class="text-sm font-semibold">{des3}</p>
        <p class="text-sm font-semibold">{des4}</p>
        <p class="underline text-sm font-semibold">{des5}</p>
        <p class="text-sm font-semibold">{des6}</p>
        <p class="text-sm font-semibold">{des7}</p>
        <p class="text-sm font-semibold">{des8}</p>
        <p class="text-sm font-semibold">{des9}</p>
        <a class="bg-[#00b285] hover:bg-[#378671] mt-10 group py-2 px-8 inline-block rounded-lg transition ease-in-out border border-transparent mb-4 lg:mb-0 md:mr-2" href={link}>
            <div class="flex items-center">
                <p class="text-white font-semibold textos">Comprar Ahora</p>
                <img class="h-4 w-4 ml-3 opacity-100 group-hover:opacity-100 transition ease-in-out group-hover:transform group-hover:translate-x-1" src="/img/SolarCartLarge2Bold.svg" alt="Icono">
            </div>
        </a>
    </div>
</section>

<style>

    .color__titulo {
        color: var(--color-primario);
    }

    /*
    .hover__planes{
        box-shadow: 0px 0px 4px 0px rgba(0,0,0,0.1);
        border-radius: 1rem;
        transition: all .2s ease-out;
    }*/

    section {
        padding: 2rem 2rem;
       /* background-color: white;*/

    }

    i{
        font-size: 12px;
        margin-left: 4px;
    }

 .textos{
    font-family: "IBM Plex Mono", monospace;
    text-transform: uppercase;
    font-weight: 600;
    font-size: 12px;
    letter-spacing: 2px;
 }
</style>
